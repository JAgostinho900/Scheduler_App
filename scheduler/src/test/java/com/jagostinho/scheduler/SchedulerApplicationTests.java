package com.jagostinho.scheduler;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SchedulerApplicationTests {

	@Test
	void contextLoads() {
		// TODO: Simple test of creating TaskModel and pushing it to the db
		/*
		 * java.sql.Date date = new java.sql.Date(0);
		 * return new TaskModel(
		 * 1L,
		 * date,
		 * date,
		 * date,
		 * date,
		 * "Title",
		 * "Description",
		 * 1,
		 * true).toString();
		 */

		// TODO: creating a task with a Timer
	}

}
